# Database (SQLite — local only; the homepage does not use the database.
# For a production deploy on Vercel, switch to a Vercel-compatible DB if you
# add data-driven features later, e.g. Postgres / PlanetScale.)
DATABASE_URL="file:./db/custom.db"
